
#include <stdio.h>
#include <stdint.h>

#define LINELENGTH 16

class Cmd {
public:
	char name[3];
	uint8_t val;

	uint8_t PutCmd() {

		char newLine[LINELENGTH];

		fgets(newLine, LINELENGTH, fptrCode);

	}
};
