
#include "main.h"

int main() {

	FILE *fptrCode;

	FILE *fptrCmd;
	FILE *fptrDat;
	FILE *fptrAdr;

	/* Open the code */
	ifstream codeFile("code.txt", "r");

	/* Create the hex files for programming the computer */
	ofstream cmdFile("cmd.hex", "w"); //cmd and dat in one file, since they are combined in one flash chip
	ofstream adrFile("adr.hex", "w");

	string newLine;
	getline(codeFile, newLine);


	CreateList();



	/* Close the files */
	codeFile.close();
	cmdFile.close();
	adrFile.close();
}
