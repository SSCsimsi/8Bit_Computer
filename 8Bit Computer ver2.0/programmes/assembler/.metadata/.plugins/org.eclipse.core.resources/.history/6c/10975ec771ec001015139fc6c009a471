
#include <stdio.h>

using namespace std;

int main() {
	FILE *fptrCode;

	FILE *fptrCmd;
	FILE *fptrDat;
	FILE *fptrAdr;

	// Open the code
	fptrCode = fopen("code.txt", "r");

	fptrCmd = fopen("cmd.hex", "w");
	fptrDat = fopen("dat.hex", "w");
	fptrAdr = fopen("adr.hex", "w");

	// Close the file
	fclose(fptrCode);
}
